import React from "react";
import { Outlet } from "react-router-dom";

const ResetPassword = () => {
  return (
    <div>
      ResetPassword
      <Outlet></Outlet>
    </div>
  );
};

export default ResetPassword;
